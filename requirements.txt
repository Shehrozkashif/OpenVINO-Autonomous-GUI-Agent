# ── RUNTIME dependencies ──────────────────────────────────────────────────────
# Everything the agent needs to RUN against an OVMS that already serves the
# models. Inference goes over HTTP (httpx) — the app imports NO ML framework
# (no torch / transformers / openvino). Installing this file alone is enough on
# a machine that receives pre-converted models.
#
# One-time model CONVERSION deps (optimum, nncf, torch, …) live in
# requirements-export.txt — install that ONLY on a machine that must convert
# UI-TARS to OpenVINO IR itself. Dev/test tools live in requirements-dev.txt.

# ── GUI framework ─────────────────────────────────────────────────────────────
PyQt6>=6.6.0

# ── Desktop control & capture ─────────────────────────────────────────────────
# Mouse/keyboard injection is raw Win32 SendInput via ctypes (desktop/input.py)
# — no third-party input library needed.
Pillow>=10.0.0
imagehash>=4.3.0
rapidocr-onnxruntime>=1.4.0
uiautomation>=2.0.18; sys_platform == "win32"   # Windows UIA Stage 0 grounding

# ── Networking (the only link to the models) ──────────────────────────────────
httpx>=0.25.0
pydantic>=2.4.0

# ── Numerics ──────────────────────────────────────────────────────────────────
numpy>=1.24.0                      # used directly by desktop/ocr.py

# ── Utilities ─────────────────────────────────────────────────────────────────
loguru>=0.7.0
pyperclip>=1.8.0   # Windows clipboard support
keyring>=24.0.0    # OS-encrypted credential storage (falls back to JSON if absent)
